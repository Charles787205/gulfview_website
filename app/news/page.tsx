import { NewsType } from "@/types";
import { NextRequest } from "next/server";
import Image from "next/image";

const NewsPage = async () => {
  return <div className="flex flex-col items-center">News Page</div>;
};

export default NewsPage;
